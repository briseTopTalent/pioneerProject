import Controller  from '../../../../../../server/src/app/controllers/notificationsController'
import { Request, Response, NextFunction } from 'express';

describe('testing index file', () => {
  test('empty string should result in zero', () => {
    //expect(Controller.FetchAll(new Request(),new Response())).toBe(0);
  });
});
